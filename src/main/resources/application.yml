s3:
  bucket:
    main: kuskus-main-images
    step: kuskus-step-images
  image:
    empty: no-image.jpeg

logging:
  level:
    org:
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG
spring:
  profiles:
    active: local